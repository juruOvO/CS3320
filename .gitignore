# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local
temp
.trae

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Claude Code local settings
.claude/

# Python cache
__pycache__/
*.pyc

# Data — original PDFs and the intermediate JSON aren't in git
# (too large; reproducible from the PDFs via scripts/build_play_json.py + build_*.py)
# but `data/` IS committed — it's the small derived layer the FastAPI server reads.
京剧剧本/
京剧剧本_json/
